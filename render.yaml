services:
  - type: web
    name: dash-ftd-frontend
    runtime: static
    buildCommand: npm run build
    staticPublishPath: ./dist

  - type: web
    name: dash-ftd-api
    runtime: node
    buildCommand: ./build-server.sh
    startCommand: node server/index.mjs
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        value: file:./dev.db 
      - key: CORS_ORIGIN
        value: https://dash-ftd-frontend.onrender.com,https://dashcliente.onrender.com